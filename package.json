{
  "name": "wealthwise-mvp",
  "version": "1.0.0",
  "description": "WealthWise MVP - AI-powered investment portfolio management",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start:backend": "npm run deploy --prefix backend",
    "start:frontend": "npm run serve --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\""
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}